This function takes a string as an argument and returns an array with the number of letters in each word in the string (in reverse order).

So if it were passed "This is a sentence" it would return an array containing [8, 1, 2, 4].
